{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "fs:allow-remove",
    "fs:read-dirs",
    "fs:read-files",
    {
      "identifier": "opener:allow-open-path",
      "allow": [
        {
          "path": "$HOME/Music/Documind/**"
        },
        { "path": "$RESOURCE/**/*" }
      ]
    },
    {
      "identifier": "fs:scope",
      "allow": [
        { "path": "$MUSIC/Documind/**/*" },
        { "path": "$APPCONFIG/**/*" },
        { "path": "$RESOURCE/**/*" }
      ]
    }
  ]
}
